node_modules/
dist/
# Root data dir only (SQLite + uploads) — an unanchored "data/" also swallowed
# client/src/quote/data/, which broke the deploy build.
/data/
*.db
*.db-shm
*.db-wal
.env
.env.*
*.log
.DS_Store
